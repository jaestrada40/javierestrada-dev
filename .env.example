DATABASE_URL=postgresql://javier:devpass123@localhost:5432/personal?schema=public
POSTGRES_USER=javier
POSTGRES_PASSWORD=devpass123
POSTGRES_DB=personal
JWT_SECRET=genera_uno_con_openssl_rand_hex_32
JWT_EXPIRES_IN=30m
# Recomendado: una clave distinta de JWT_SECRET para cifrar el secreto MFA.
# Genera una con: openssl rand -hex 32
MFA_ENCRYPTION_KEY=
ADMIN_USERNAME=javier
# Obligatoria, mínimo 10 caracteres — el seed falla si no la defines
ADMIN_PASSWORD=
PORT=3000
# Orígenes permitidos para CORS, separados por coma
CORS_ORIGINS=https://javierestrada.dev,http://localhost:4200
